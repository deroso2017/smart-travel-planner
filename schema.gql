# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type TripType {
  id: ID!
  title: String!
  destination: String!
  startDate: String!
  endDate: String!
  userId: String
}

type UserType {
  id: ID!
  email: String!
}

type AuthResponse {
  accessToken: String!
}

type Query {
  users: [UserType!]!
  userByEmail(email: String!): UserType
  trips: [TripType!]!
  trip(id: ID!): TripType
}

type Mutation {
  register(input: UserDto!): UserType!
  login(input: UserDto!): AuthResponse!
  createTrip(input: CreateTripDto!): TripType!
  updateTrip(input: UpdateTripDto!): TripType
  deleteTrip(id: ID!): TripType
}

input UserDto {
  email: String!
  password: String!
}

input CreateTripDto {
  title: String!
  destination: String!
  startDate: String!
  endDate: String!
  userId: String
}

input UpdateTripDto {
  title: String
  destination: String
  startDate: String
  endDate: String
  userId: String
  id: ID!
}